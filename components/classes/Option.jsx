import React from 'react';

const Option = ({ title }) => {
    return (
        <div className="option">
            <h3 className="option__title">{title}</h3>
        </div>
    )
}

export default Option;
